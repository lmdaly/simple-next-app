{
  "name": "learn-starter",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p 8080",
    "build": "lscpu -p | egrep -v \"^#\" | sort -u -t, -k 2,4 | wc -l; next build",
    "start": "next start -p 8080",
    "stop": "next stop"
  },
  "dependencies": {
    "next": "^10.0.0",
    "react": "17.0.1",
    "react-dom": "17.0.1"
  }
}
